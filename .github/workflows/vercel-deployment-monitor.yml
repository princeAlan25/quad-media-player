name: Vercel Deployment Monitor

on:
  deployment_status:

permissions:
  pull-requests: read
  deployments: read
  contents: read
  statuses: write

jobs:

  # ─────────────────────────────────────────────
  # COMMON: Resolve PR from SHA
  # ─────────────────────────────────────────────

  resolve-pr:
    runs-on: ubuntu-latest
    outputs:
      found: ${{ steps.pr.outputs.found }}
      merged: ${{ steps.pr.outputs.merged }}
      number: ${{ steps.pr.outputs.number }}
      title: ${{ steps.pr.outputs.title }}
      author: ${{ steps.pr.outputs.author }}
      url: ${{ steps.pr.outputs.url }}

    steps:
      - name: Resolve PR
        id: pr
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.payload.deployment.sha;

            const { data: pulls } =
              await github.rest.repos.listPullRequestsAssociatedWithCommit({
                owner: context.repo.owner,
                repo: context.repo.repo,
                commit_sha: sha,
              });

            if (!pulls.length) {
              core.setOutput("found", "false");
              return;
            }

            const pr = pulls[0];

            core.setOutput("found", "true");
            core.setOutput("merged", pr.merged_at ? "true" : "false");
            core.setOutput("number", String(pr.number));
            core.setOutput("title", pr.title);
            core.setOutput("author", pr.user.login);
            core.setOutput("url", pr.html_url);

  # ─────────────────────────────────────────────
  # PREVIEW → SMOKE SUITE
  # ─────────────────────────────────────────────

  preview-smoke:
    runs-on: ubuntu-latest
    needs: resolve-pr

    if: >
      contains(fromJSON('["success","failure","error"]'), github.event.deployment_status.state)
      && needs.resolve-pr.outputs.found == 'true'
      && needs.resolve-pr.outputs.merged != 'true'

    steps:

      - name: Mark pending status
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.payload.deployment.sha,
              state: "pending",
              context: "Learnershood QA / smoke",
              description: "Smoke suite running...",
              target_url: "https://github.com/princeAlan25/learnershood-quality/actions"
            });

      - name: Dispatch smoke suite
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.QA_DISPATCH_TOKEN }}
          repository: princeAlan25/learnershood-quality
          event-type: product-pr-preview-ready
          client-payload: |
            {
              "deployment": {
                "status": "${{ github.event.deployment_status.state }}",
                "environment": "${{ github.event.deployment.environment }}",
                "url": "${{ github.event.deployment_status.target_url }}",
                "sha": "${{ github.event.deployment.sha }}"
              },
              "pull_request": {
                "number": "${{ needs.resolve-pr.outputs.number }}",
                "title": "${{ needs.resolve-pr.outputs.title }}",
                "author": "${{ needs.resolve-pr.outputs.author }}",
                "url": "${{ needs.resolve-pr.outputs.url }}"
              },
              "repository": {
                "owner": "${{ github.repository_owner }}",
                "full_name": "${{ github.repository }}"
              },
              "suite": {
                "name": "smoke",
                "trigger": "preview"
              }
            }

  # ─────────────────────────────────────────────
  # PRODUCTION → POST DEPLOY SUITE
  # ─────────────────────────────────────────────

  post-deploy:
    runs-on: ubuntu-latest
    needs: resolve-pr

    if: >
      github.event.deployment_status.state == 'success' &&
      (
        contains(github.event.deployment.environment, 'production') ||
        contains(github.event.deployment.environment, 'Production') ||
        contains(github.event.deployment.environment, 'PRODUCTION')
      ) &&
      needs.resolve-pr.outputs.found == 'true' &&
      needs.resolve-pr.outputs.merged == 'true'

    steps:

      - name: Mark pending status
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.payload.deployment.sha,
              state: "pending",
              context: "Learnershood QA / post-deploy",
              description: "Post-deploy suite running...",
              target_url: "https://github.com/princeAlan25/learnershood-quality/actions"
            });

      - name: Dispatch post deploy suite
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.QA_DISPATCH_TOKEN }}
          repository: princeAlan25/learnershood-quality
          event-type: product-post-deploy-ready
          client-payload: |
            {
              "deployment": {
                "environment": "${{ github.event.deployment.environment }}",
                "url": "${{ github.event.deployment_status.target_url }}",
                "sha": "${{ github.event.deployment.sha }}"
              },
              "pull_request": {
                "number": "${{ needs.resolve-pr.outputs.number }}",
                "title": "${{ needs.resolve-pr.outputs.title }}",
                "author": "${{ needs.resolve-pr.outputs.author }}",
                "url": "${{ needs.resolve-pr.outputs.url }}"
              },
              "repository": {
                "owner": "${{ github.repository_owner }}",
                "full_name": "${{ github.repository }}"
              },
              "suite": {
                "name": "post-deploy",
                "trigger": "production"
              }
            }
